[build]
builder = "nixpacks"

[deploy]
startCommand = "node scripts/railway-start.js"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[deploy.envs]
NODE_ENV = "production" 
